package org.sjhstudio.fastcampus.part0.kotlin

/**
 * 2. 변수(Variable)
 */

fun main() {
    // 변수
    // val = value (값)
    // var = variable (변경가능한)
    val a: Int = 3
    var b: Int = 10
    b = 30

    val name = "서준형"    // 타입 추론 기능으로 변수 선언시 타입을 명시하지 않아도 된다.
}